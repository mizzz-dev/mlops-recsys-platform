# セキュリティ方針

## データ

- 実ユーザーの個人情報は使わない
- OSSでは合成データのみを扱う
- ログに機密情報を出さない

## API

- MVPでは認証を対象外とする
- 外部公開時はAPI key、OIDC、またはCloud Run IAMを検討する
- 入力値は `user_id` と `limit` の時点で検証する

## シークレット

- シークレットをリポジトリに含めない
- GitHub Actionsでは必要になった段階で環境別secretを使う
